<h1>ニコニコタグ検索</h1>


      <%= form_tag @product, :method => 'get' do %>
  <p>
          <%= text_field_tag :search, params[:search] %>
          <%= submit_tag "Vocaloidで検索", :name => 'vocaloid' %>
         <%= submit_tag "スポーツで検索", :name => 'sports' %> 
         <%= submit_tag "アニメで検索", :name => 'anime' %>
  </p>
<% end %>


<p>
<% if @search_word.nil? then %>
<p>
検索タグは"ひとつ"にしてね (○ = > [ニコニコ動画] × => [youtube_ニコニコ動画])
</p>
<% end %>

検索タグ:<%= @search_word %>　検索ヒット数 <%= @hits %> 件

</p>

</p>


<table>
  <thead>
    <tr>
      <th>Tag</th>
      <th>Late</th>
      <th>Tag_value</th>
   </tr>
  </thead>

  <tbody>
   <% if !@search_word.nil? then %> 
    <% @products[2].each do |kinji, kinji2, kinji3| %>
      <tr>
        <td> <%= link_to kinji, "http://search.nicovideo.jp/video/tag/#{kinji}", :target => "_blank" %></td>
        <td><%= kinji2 %></td>
        <td><%= kinji3 %></td>
     </tr>
    <% end %>
   <% end %>
  </tbody>
</table>
<br>


<%= link_to 'New Product', new_product_path %>
